import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.howtion.live2dagenttools",
  appName: "Live2DAgentTools",
  webDir: "../../apps/web/dist",
};

export default config;
