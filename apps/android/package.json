{
  "name": "@live2dagenttools/android",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo \"Android shell: run npm run android:init first, then use Capacitor/Android Studio\"",
    "typecheck": "echo \"No Android TS sources yet\"",
    "cap:add:android": "npx cap add android",
    "cap:open:android": "npx cap open android",
    "cap:sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
